# Git 提交信息（Source Control AI 生成）

当 Cursor 为本仓库自动生成 Git commit message（Source Control 提交框旁的 ✨）时：

- **必须**使用**简体中文**撰写提交说明。
- 可使用 `feat:` / `fix:` / `chore:` 等前缀，冒号后的描述须为中文。
- 不要生成英文提交信息。

示例：`fix: 修复用户列表分页参数错误`
